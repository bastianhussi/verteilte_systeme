const Layout = (props) => (
  <>
    <style jsx global>
      {`
        
            body {
                background-color: blue;
            }

        `}
    </style>
  </>
);

export default Layout;
